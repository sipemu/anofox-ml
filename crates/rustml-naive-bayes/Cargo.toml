[package]
name = "rustml-naive-bayes"
version = "0.1.0"
edition = "2021"

[dependencies]
rustml-core = { path = "../rustml-core" }
ndarray = { version = "0.16", features = ["serde"] }
num-traits = "0.2"
serde = { workspace = true }

[dev-dependencies]
approx = "0.5"
